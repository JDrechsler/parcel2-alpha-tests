import { h, Component } from "preact";

class LazyPage extends Component {
  render() {
    return (
      <section>
        <h2>Lazy</h2>
      </section>
    );
  }
}

export default LazyPage;
